import numpy as np

from display3d.sprite.base_color_sprite import BaseColorSprite3D
from display3d.sprite.base_text_sprite import BaseTextSprite3D


class BaseFlagSprite(BaseColorSprite3D):

    def __init__(self, scene, text, color: tuple = (1.0, 1.0, 1.0)):
        super().__init__(scene, color)

        self.text_sprite = BaseTextSprite3D(self.scene3D, text=text, font_size=64, color=(1.0, 1.0, 0.0))
        self.text_sprite.scaleX = 2
        self.text_sprite.scaleY = 2

    def upData(self):
        super().upData()
        self.text_sprite.x = self.x
        self.text_sprite.y = self.y + 10
        self.text_sprite.z = self.z
        self.text_sprite.upData()

    def makeVertices(self, color: tuple = (1.0, 1.0, 1.0)):
        r, g, b = color
        self.vertices = np.array([
            # 立方体旗杆（6个面，每个面2个三角形）
            # 前面 - 三角形1
            -0.10, -0.5, -0.10, r, g, b, 0.0, 0.0,  # 前面左下
            0.10, -0.5, -0.10, r, g, b, 1.0, 0.0,  # 前面右下
            0.10, 4.5, -0.10, r, g, b, 1.0, 1.0,  # 前面右上
            # 前面 - 三角形2
            0.10, 4.5, -0.10, r, g, b, 1.0, 1.0,  # 前面右上
            -0.10, 4.5, -0.10, r, g, b, 0.0, 1.0,  # 前面左上
            -0.10, -0.5, -0.10, r, g, b, 0.0, 0.0,  # 前面左下

            # 后面 - 三角形1
            0.10, -0.5, 0.10, r, g, b, 0.0, 0.0,  # 后面右下
            -0.10, -0.5, 0.10, r, g, b, 1.0, 0.0,  # 后面左下
            -0.10, 4.5, 0.10, r, g, b, 1.0, 1.0,  # 后面左上
            # 后面 - 三角形2
            -0.10, 4.5, 0.10, r, g, b, 1.0, 1.0,  # 后面左上
            0.10, 4.5, 0.10, r, g, b, 0.0, 1.0,  # 后面右上
            0.10, -0.5, 0.10, r, g, b, 0.0, 0.0,  # 后面右下

            # 左面 - 三角形1
            -0.10, -0.5, 0.10, r, g, b, 0.0, 0.0,  # 左面后下
            -0.10, -0.5, -0.10, r, g, b, 1.0, 0.0,  # 左面前下
            -0.10, 4.5, -0.10, r, g, b, 1.0, 1.0,  # 左面前上
            # 左面 - 三角形2
            -0.10, 4.5, -0.10, r, g, b, 1.0, 1.0,  # 左面前上
            -0.10, 4.5, 0.10, r, g, b, 0.0, 1.0,  # 左面后上
            -0.10, -0.5, 0.10, r, g, b, 0.0, 0.0,  # 左面后下

            # 右面 - 三角形1
            0.10, -0.5, -0.10, r, g, b, 0.0, 0.0,  # 右面前下
            0.10, -0.5, 0.10, r, g, b, 1.0, 0.0,  # 右面后下
            0.10, 4.5, 0.10, r, g, b, 1.0, 1.0,  # 右面后上
            # 右面 - 三角形2
            0.10, 4.5, 0.10, r, g, b, 1.0, 1.0,  # 右面后上
            0.10, 4.5, -0.10, r, g, b, 0.0, 1.0,  # 右面前上
            0.10, -0.5, -0.10, r, g, b, 0.0, 0.0,  # 右面前下

            # 底面 - 三角形1
            -0.10, -0.5, 0.10, r, g, b, 0.0, 0.0,  # 底面后左
            0.10, -0.5, 0.10, r, g, b, 1.0, 0.0,  # 底面后右
            0.10, -0.5, -0.10, r, g, b, 1.0, 1.0,  # 底面前右
            # 底面 - 三角形2
            0.10, -0.5, -0.10, r, g, b, 1.0, 1.0,  # 底面前右
            -0.10, -0.5, -0.10, r, g, b, 0.0, 1.0,  # 底面前左
            -0.10, -0.5, 0.10, r, g, b, 0.0, 0.0,  # 底面后左

            # 顶面 - 三角形1
            -0.10, 4.5, -0.10, r, g, b, 0.0, 0.0,  # 顶面前左
            0.10, 4.5, -0.10, r, g, b, 1.0, 0.0,  # 顶面前右
            0.10, 4.5, 0.10, r, g, b, 1.0, 1.0,  # 顶面后右
            # 顶面 - 三角形2
            0.10, 4.5, 0.10, r, g, b, 1.0, 1.0,  # 顶面后右
            -0.10, 4.5, 0.10, r, g, b, 0.0, 1.0,  # 顶面后左
            -0.10, 4.5, -0.10, r, g, b, 0.0, 0.0,  # 顶面前左

            # 正三角形旗帜（从杆子侧面水平延伸，高度为杆子一半=2.5）

            # 底边在杆子上，顶点向右延伸
            # 三角形1（上半部分）
            0.10, 2.0, 0.0, r, g, b, 0.0, 0.0,  # 底边下点（杆子上）
            0.10, 4.5, 0.0, r, g, b, 0.0, 1.0,  # 底边上点（杆子上）
            2.55, 3.25, 0.0, r, g, b, 1.0, 0.5,  # 顶点（向右延伸）
            # 三角形2（下半部分）
            0.10, 2.0, 0.0, r, g, b, 0.0, 0.0,  # 底边下点（杆子上）
            2.55, 3.25, 0.0, r, g, b, 1.0, 0.5,  # 顶点（向右延伸）
            0.10, 4.5, 0.0, r, g, b, 0.0, 1.0,  # 底边上点（杆子上）
        ], dtype=np.float32)
